use std::mem;

fn main(){
    let xs: [i32; 5] = [1, 2, 3, 4, 5]; //fixed-size array
                                        

    //All elements ca be initialized at the same value
    let ys: [i32; 500] = [0; 500];
}
